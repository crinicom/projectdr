<div class="container">
        <form action="/task/update" method="POST" id="new-task-form" class="form-new-obj">
        <h2 class="form-signin-heading">Create task</h2>
    
        <% if (flash && flash.err) { %>
            <ul class="alert alert-success">
                <li>Existe flash</li>
                <% Object.keys(flash.err).forEach(function(error) {%>
                <li><%- JSON.stringify(flash.err[error]) %></li>
                <% }); %>
            </ul>
    
        <% } %>
        <!-- <input type="hidden" name="belongs_to_obj" value="<%//=objective.id%>" /> -->
        <input type="hidden" name="belongs_to_project" value="<%=belongs_to_project%>" />
         
        <div class="control-group">
            <input type="text" class="form-control" placeholder="Assign responsible" name="responsible" autofocus ></br>
        </div>
        
        <!-- <div class="control-group">
            <input type="number" class="form-control" placeholder="%advance" name="status"></br>
        </div>
     -->
        <br/>
        
        <input type="submit" class="btn btn-lg btn-primary btn-block" value="Assign task">
        <input type="hidden" name="_csrf" value="<%=_csrf%>" />
        </form>
    </div>
    
